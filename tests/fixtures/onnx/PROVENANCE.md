# Test fixture: `dummy_identity.onnx`

| Field | Value |
| --- | --- |
| **Origin** | Generated in-repo by `scripts/gen_dummy_identity_onnx.py` using the [ONNX](https://github.com/onnx/onnx) Python package (Apache-2.0). |
| **License** | The ONNX file format and generated graph are covered by the same permissive licensing as the `onnx` tool used to emit it; the graph contains only the standard **Identity** operator (opset 13). |
| **Purpose** | Integration test for ONNX Runtime session load and one forward pass in `PhonemeOnnxRunner`. |
| **Modifications** | None after generation. Re-run the script to regenerate. |
